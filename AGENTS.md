# DevCadence Project

This project contains the DevCadence protocol skill.

## Quick Reference
- Skill: `skills/devcadence/SKILL.md` — the protocol
- Command: `commands/devcadence.md` — base invocation

## Build
```bash
pip install -e ".[dev]"
pytest tests/
```
